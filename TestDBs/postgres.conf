work_mem = '64MB'
maintenance_work_mem = '128MB'
shared_buffers = '256MB'

# Enable listening on all IP addresses or specific addresses
listen_addresses = '*'

# Configure max connections (optional based on your needs)
max_connections = 100